{
  "name": "higherpays-backend",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "HigherPays — creator agency operating system (backend API)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/util/migrate.js",
    "seed": "node src/util/seed.js",
    "setup": "node src/util/migrate.js && node src/util/seed.js",
    "test": "node --test test/*.test.js",
    "test:db": "node --test test/engine.test.js",
    "test:integration": "node --test test/integration/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.5",
    "pino": "^9.14.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
