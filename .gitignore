# Dependencies
node_modules/
**/node_modules/

# Build output
dist/
dist-ssr/
**/dist/
**/dist-ssr/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.sw?

# Local env
*.local
.env
.env.*
!.env.example

# Deploy target (host + key path) - machine-local, never committed
deploy/deploy.env

# Coverage
coverage/
.git-commit-msg.tmp

# SSH keys - never commit
.ssh/
*.pem

# Local docker overrides (dev-only; EC2 pulls only docker-compose.yml)
docker-compose.override.yml

# Local skill installs — machine-local, not part of the app
.agents/
skills-lock.json
